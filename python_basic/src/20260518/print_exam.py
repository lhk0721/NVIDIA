#파이썬에서는 문자열은 ''(작은따옴표), ""(큰따옴표)로 표현한다.
print("python") #모니터에 문자열을 출력하는 함수
print('python')
print("문자열 안의 '작은따옴표'")

# 줄바꿈 역할의 특수문자: \n (개행문자)
print("줄바꿈 역할의 \n 특수문자", end="") # 함수 파라미터에 아무것도 주어지지 않으면 default 파라미터가 적용된다.
# default는 개행 (end = "\n")으로 되어있다. (마우스오버>선언위치로 이동)
print("AI programming")

